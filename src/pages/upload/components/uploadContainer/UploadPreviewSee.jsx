import classNames from 'classnames/bind';
import styles from './UploadPreviewSee.module.scss';

const cx = classNames.bind(styles);

const UploadPreviewSee = () => {
  return <div></div>;
};

export default UploadPreviewSee;
